# D3 Memory Notes

## ??

D3 ?????????????? prompt?????????????????????????????? typed memory?

?? D3+ ????? memory subsystem v1?

```text
conversation
  -> ReflectionCapability ???????? reflect()
  -> LLM ?????? typed memories
  -> parser ?? JSON / memory_type / content
  -> exact dedupe ????????
  -> semantic retrieval ??????
  -> LLM ?? skip / replace / keep_both
  -> db ?? add_memory() ? update_memory()
  -> retrieve_memories() ??????????????????
  -> MemoryCapability ?????????? last_used_at / access_count
```

## ???

### 1. Typed memory reflection

???`infra/reflection.py`

- `REFERENCE_PROMPT` ?? LLM ??? JSON array?
- ?? memory ?????
  - `memory_type`: `fact / preference / reference`
  - `content`: ??????????
- `parse_typed_memories()` ????
  - ?? JSON
  - ? list ??
  - ? dict item
  - ?? memory_type
  - ? content
  - `? / none / null`

?????LLM ????????????????

### 2. Exact dedupe

???`infra/db.py`?`infra/reflection.py`

- `memory_exists(content, memory_type)` ??????????????????
- ?????`reflect()` ???????????

???????????

```text
?????? mini-coder ??
?????? mini-coder ??
```

### 3. Semantic merge decision

???`infra/memory.py`?`infra/reflection.py`

- `find_similar_memories()` ????????????????
- `decide_memory_merge()` ???????????? LLM ???
- LLM ?????? action?
  - `skip`: ?????????????
  - `replace`: ???????????????
  - `keep_both`: ???????
- `parse_merge_decision()` ????
  - ????? JSON object
  - action ????????
  - `target_content` ?????????

?????LLM ?????????????????????

### 4. Replace execution

???`infra/db.py`?`infra/reflection.py`

- `update_memory(memory_id, content, memory_type, embedding)` ???? id ????????
- `reflect()` ? action ? `replace` ??
  - ?? `target_content` ???????
  - ?????? `id` ?? `update_memory()`
- ?? LLM ????????????????? `keep_both`????????

### 5. Memory lifecycle fields

???`infra/db.py`

`memories` ??????

```text
created_at     ???????
updated_at     ???????/?????
last_used_at   ?????????????
access_count   ?????????
```

`init_db()` ????????

```text
_memory_column_exists()
_add_memory_column_if_missing()
```

????????????????????

### 6. Usage tracking

???`capabilities/builtin.py`?`infra/db.py`

- `retrieve_memories()` ?? memory id?
- `MemoryCapability` ?????????? system prompt ????? `mark_memories_used(memory_ids)`?
- `mark_memories_used()` ????

```text
last_used_at = CURRENT_TIMESTAMP
access_count = access_count + 1
```

???????????????????????????

### 7. Decay scoring and rerank

???`infra/memory.py`

`retrieve_memories()` ?????????????????

```text
final_score = semantic_score * decay_score * usage_boost
```

??????

```python
HALF_LIFE_DAYS = {
    "fact": 180,
    "preference": 90,
    "reference": 30,
}
```

???

```text
fact        ??????????
preference  ?????????????
reference   ???????????????
```

???????

```text
usage_boost = 1 + 0.05 * log(1 + access_count)
```

?????????????????????

### 8. Time-aware merge prompt

???`infra/reflection.py`

`MERGE_DECISION_PROMPT` ??????????????????? LLM?

```text
created_at
updated_at
last_used_at
access_count
```

LLM ?? replace ??????

- ?????????????? replace?
- ??????????????????????? replace?
- ????????????? keep_both?

## ????

D3+ ?????????????? memory??????????????

- ?? context budget???????????? `k` ??????? token/???????
- ?? provenance????? memory ???? source message id?
- ?? active/inactive versioning?replace ???????????????????
- ?? confidence / importance?reflection ??????????????????
- ?? forget API????????????/?????
- ?? memory eval??????????????????????token ???

## ???????

?????? memory subsystem?????????

### ??????Context budget

???????? memory ?? prompt?

???

```text
max_items
max_chars / max_tokens
per_type_limit
```

?????

```text
retrieve top 20
  -> ? final_score ??
  -> ???? N ?
  -> ????????????
  -> ??????? memory ? mark_memories_used
```

### ??????Provenance

?????????????

?????

```text
source_conversation_id
source_message_ids
created_from: reflection / manual / import
```

?????????????????????????????

### ??????Active / inactive versioning

???replace ????????????????

?????

```text
status: active / inactive
superseded_by
```

??????? LLM ?????????????

### ??????Confidence / importance

????????????????????

?? reflection ???

```json
{
  "memory_type": "preference",
  "content": "????????????",
  "confidence": 0.9,
  "importance": 0.8
}
```

??????

```text
final_score = semantic_score * decay_score * usage_boost * confidence * importance
```

### ??????Memory evaluation

???????? memory ?????

????

```text
???
??????
??????
prompt token ??
?????
```

??? ablation?

```text
? memory
?????
???? + type
???? + type + decay
???? + type + decay + merge
```

## ????

???????????

> ????????????? reflection?? LLM ?????????????????? content ? memory_type???????????????????????????????????????????? LLM ?? skip?replace ?? keep_both?LLM ?????????????? JSON ??? action ????????????????????? memory id ???????????????????????????????????????????? final_score??????????????
